# JWT Auth Secret
SECRET_KEY=replace-with-a-secure-secret-key

# OpenAI API Key (Required for NL->SQL, Insights, Visualization config)
OPENAI_API_KEY= 

# Database URL (Defaults to sqlite:///./insightai.db if not provided)
# DATABASE_URL=postgresql://user:password@localhost/insightai
